services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tools-collection
    ports:
      - "8080:80"
    volumes:
      - ./data:/usr/share/nginx/html/data:ro
    restart: unless-stopped
